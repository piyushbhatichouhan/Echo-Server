name: Deploy Echo Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Sync repository
        run: |
          cd /srv/echo
          git fetch origin
          git reset --hard origin/main
          git clean -fd
